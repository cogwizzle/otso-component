import React from 'react'
import { Controlled<%= props.pascalCase(props.inputType) %> } from 'component'

export const <%= props.pascalCase(props.inputName) %> = () => {
  return (
    <Controlled<%= props.pascalCase(props.inputType) %>
      label='<%= props.pascalCase(props.inputName) %>'
      name='<%= props.dashCase(props.inputName) %>'<%= (props.inputType === 'select') ? `
      options={[]}` : '' %>
    />
  )
}